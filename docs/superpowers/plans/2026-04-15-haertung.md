# Härtung & Stabilisierung – Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sicherheit, Datenintegrität und Wartbarkeit des DLRG Bestellsystems verbessern — keine neuen Features, nur Härtung.

**Architecture:** Jede Aufgabe ist ein isoliertes Modul oder eine gezielte Änderung. Neue Hilfsmodule (`storage.js`, `validation.js`, `dom.js`, `audit.js`) werden testgetrieben entwickelt. Bestehende Logikmodule werden punktuell geändert. HTML-Seiten werden mechanisch auf den Storage-Wrapper umgestellt.

**Tech Stack:** Vanilla JS (ES Modules), HTML-Testseiten (kein Framework), localStorage/sessionStorage, bestehende Testinfrastruktur in `tests/test_*.html`

**Parallelisierung:**
- Wave 1 (parallel): Tasks 1, 2, 3, 4
- Wave 2 (unabhängig): Task 5 (storage.js)
- Wave 3 (nach Task 5): Tasks 6, 7
- Wave 4 (nach Task 5): Tasks 8, 9

---

## Bestehende Muster — zum Nachschlagen

### Test-Datei-Vorlage (wie `tests/test_berechnung.html`):
```html
<!DOCTYPE html>
<html lang="de">
<head><meta charset="UTF-8"><title>Tests – [Modulname]</title></head>
<body><pre id="out"></pre>
<script type="module">
import { ... } from '../src/....js';
let ok = 0, fail = 0;
function assert(label, cond) {
  const el = document.getElementById('out');
  el.textContent += (cond ? '✅' : '❌') + ' ' + label + '\n';
  cond ? ok++ : fail++;
}
// Tests hier
document.getElementById('out').textContent += `\n${ok} passed, ${fail} failed`;
</script></body></html>
```

### Storage-Pattern (aktuell, in fast jeder HTML-Datei):
```js
// Lesen (Inline-Helper in jeder HTML-Datei):
function ladeJson(key) { try { return JSON.parse(localStorage.getItem(key) || 'null'); } catch { return null; } }
// Schreiben:
localStorage.setItem(STORAGE_KEY_B, JSON.stringify(bestellungen));
```

### Storage-Keys im Projekt:
- `'lo_einstellungen'` → Einstellungen (OG-Daten, NC-Credentials, Mitglieder)
- `'lo_artikel'` → Artikelkatalog
- `'lo_bestellungen'` → Sammelbestellungen mit Wünschen, Positionen, Rechnungen

---

## Task 1: Förderlogik clamping in `berechnung.js`

**Kann parallel zu Tasks 2, 3, 4 laufen.**

**Files:**
- Modify: `src/berechnung.js`
- Modify: `tests/test_berechnung.html` (Tests ergänzen)

**Problem:** `berechneFoerderung()` kann negativen `mitglied`-Wert zurückgeben wenn
`bv + lv + og > einzelpreis * menge`. Aktueller Code in `src/berechnung.js:34`:
```js
const mitglied = runde(gesamt - bv - lv - og);
```

- [ ] **Schritt 1: Failing-Test schreiben**

Öffne `tests/test_berechnung.html`. Füge am Ende der vorhandenen Tests ein:

```js
// Clamping: mitglied darf nie negativ sein
assert('mitglied nie negativ wenn Förderung > Preis', (() => {
  const artikel = { einzelpreis: 5, bvFoerderung: 4, lvFoerderung: 4, ogFoerderung: 0, ogUebernimmtRest: false };
  const res = berechneFoerderung(artikel, 1);
  return res.mitglied >= 0;
})());

assert('mitglied = 0 wenn Summe Förderungen = Preis', (() => {
  const artikel = { einzelpreis: 10, bvFoerderung: 5, lvFoerderung: 5, ogFoerderung: 0, ogUebernimmtRest: false };
  const res = berechneFoerderung(artikel, 1);
  return res.mitglied === 0;
})());
```

- [ ] **Schritt 2: Test im Browser öffnen — muss FAIL**

Öffne `tests/test_berechnung.html` im Browser. Erwartet: `❌ mitglied nie negativ wenn Förderung > Preis`.

- [ ] **Schritt 3: Fix implementieren**

In `src/berechnung.js`, ersetze Zeile 34:
```js
const mitglied = runde(gesamt - bv - lv - og);
```
durch:
```js
const mitglied = Math.max(0, runde(gesamt - bv - lv - og));
```

- [ ] **Schritt 4: Test im Browser — muss PASS**

Alle vorhandenen Tests müssen weiterhin grün sein.

- [ ] **Schritt 5: Commit**

```bash
git add src/berechnung.js tests/test_berechnung.html
git commit -m "fix: mitglied-Anteil in berechneFoerderung nie negativ (clamping)"
```

---

## Task 2: Datums-Sortierung auf Date-Objekte umstellen

**Kann parallel zu Tasks 1, 3, 4 laufen.**

**Files:**
- Modify: `src/stunden.js:83`
- Modify: `tests/test_stunden.html`

**Problem:** `stunden.js:83` nutzt `localeCompare` für Datums-Strings. Funktioniert für ISO-Format, ist aber fragil bei abweichenden Formaten.

- [ ] **Schritt 1: Failing-Test schreiben**

Öffne `tests/test_stunden.html`. Füge hinzu:

```js
// Sortierung: Datum-Reihenfolge korrekt auch wenn nicht alphabetisch ISO
assert('verechneSchuld: älteste Schuld zuerst getilgt (Datumsreihenfolge)', (() => {
  const bestellungen = [
    { datum: '2025-06-01', rechnungen: [{ id: 'r2', nummer: 'R_2025_06_001', datum: '2025-06-15', mitgliedId: 'alice', erwartetEinsatzstunden: 6 }] },
    { datum: '2025-01-01', rechnungen: [{ id: 'r1', nummer: 'R_2025_01_001', datum: '2025-01-10', mitgliedId: 'alice', erwartetEinsatzstunden: 3 }] },
  ];
  const { verrechnungen } = verechneSchuld('alice', bestellungen, 3, { stundenRate: { stunden: 3, euro: 10 } });
  // Älteste Rechnung (Januar) muss zuerst verrechnet sein und vollständig getilgt
  return verrechnungen[0].rechnungNummer === 'R_2025_01_001' && verrechnungen[0].offenStunden === 0;
})());
```

- [ ] **Schritt 2: Test im Browser — beobachten ob PASS oder FAIL**

(Kann sein dass er bereits passt wegen ISO-Format — trotzdem Schritt 3 durchführen.)

- [ ] **Schritt 3: Sort auf Date umstellen**

In `src/stunden.js:83`, ersetze:
```js
schulden.sort((a, b) => a.rechnungDatum.localeCompare(b.rechnungDatum));
```
durch:
```js
schulden.sort((a, b) => new Date(a.rechnungDatum).getTime() - new Date(b.rechnungDatum).getTime());
```

- [ ] **Schritt 4: Alle Tests im Browser — müssen PASS**

- [ ] **Schritt 5: Commit**

```bash
git add src/stunden.js tests/test_stunden.html
git commit -m "fix: Schulden-Sortierung auf Date-Vergleich umgestellt statt localeCompare"
```

---

## Task 3: `validation.js` — zentrale Validierung

**Kann parallel zu Tasks 1, 2, 4 laufen.**

**Files:**
- Create: `src/validation.js`
- Create: `tests/test_validation.html`

- [ ] **Schritt 1: Test-Datei anlegen**

Erstelle `tests/test_validation.html`:

```html
<!DOCTYPE html>
<html lang="de">
<head><meta charset="UTF-8"><title>Tests – validation</title></head>
<body><pre id="out"></pre>
<script type="module">
import { validateWunsch, validatePosition, validateArtikel } from '../src/validation.js';
let ok = 0, fail = 0;
function assert(label, cond) {
  document.getElementById('out').textContent += (cond ? '✅' : '❌') + ' ' + label + '\n';
  cond ? ok++ : fail++;
}

// validateWunsch
assert('validateWunsch: ok bei gültigem Eintrag', validateWunsch({ mitgliedId: 'alice', artikelNr: '12345', menge: 2 }).ok === true);
assert('validateWunsch: fehler bei fehlender mitgliedId', validateWunsch({ artikelNr: '12345', menge: 2 }).ok === false);
assert('validateWunsch: fehler bei fehlender artikelNr', validateWunsch({ mitgliedId: 'alice', menge: 2 }).ok === false);
assert('validateWunsch: fehler bei menge 0', validateWunsch({ mitgliedId: 'alice', artikelNr: '12345', menge: 0 }).ok === false);
assert('validateWunsch: fehler bei negativer menge', validateWunsch({ mitgliedId: 'alice', artikelNr: '12345', menge: -1 }).ok === false);
assert('validateWunsch: fehler bei Dezimalmenge', validateWunsch({ mitgliedId: 'alice', artikelNr: '12345', menge: 1.5 }).ok === false);

// validatePosition
assert('validatePosition: ok bei gültiger Position', validatePosition({ artikelNr: '12345', menge: 1, einzelpreis: 9.99 }).ok === true);
assert('validatePosition: fehler bei menge 0', validatePosition({ artikelNr: '12345', menge: 0, einzelpreis: 9.99 }).ok === false);
assert('validatePosition: fehler bei negativem Preis', validatePosition({ artikelNr: '12345', menge: 1, einzelpreis: -1 }).ok === false);
assert('validatePosition: ok bei Preis 0', validatePosition({ artikelNr: '12345', menge: 1, einzelpreis: 0 }).ok === true);

// validateArtikel
assert('validateArtikel: ok bei vollständigem Artikel', validateArtikel({ artikelNr: '12345', name: 'T-Shirt', einzelpreis: 19.99 }).ok === true);
assert('validateArtikel: fehler bei fehlendem Namen', validateArtikel({ artikelNr: '12345', einzelpreis: 19.99 }).ok === false);
assert('validateArtikel: fehler bei negativem Preis', validateArtikel({ artikelNr: '12345', name: 'T-Shirt', einzelpreis: -5 }).ok === false);
assert('validateArtikel: fehler bei fehlender artikelNr', validateArtikel({ name: 'T-Shirt', einzelpreis: 19.99 }).ok === false);

document.getElementById('out').textContent += `\n${ok} passed, ${fail} failed`;
</script></body></html>
```

- [ ] **Schritt 2: Test im Browser — muss FAIL (Modul existiert nicht)**

- [ ] **Schritt 3: `src/validation.js` implementieren**

```js
/**
 * validation.js
 * Zentrale Eingangsvalidierung für Domänenobjekte.
 *
 * Öffentliche API:
 *   validateWunsch(w)    → { ok, fehler? }
 *   validatePosition(p)  → { ok, fehler? }
 *   validateArtikel(a)   → { ok, fehler? }
 */

function err(fehler) { return { ok: false, fehler }; }
const OK = { ok: true };

/**
 * Prüft einen Mitgliederwunsch.
 * @param {{ mitgliedId?, artikelNr?, menge? }} w
 */
export function validateWunsch(w) {
  if (!w.mitgliedId || !String(w.mitgliedId).trim()) return err('mitgliedId darf nicht leer sein');
  if (!w.artikelNr  || !String(w.artikelNr).trim())  return err('artikelNr darf nicht leer sein');
  if (typeof w.menge !== 'number' || !Number.isInteger(w.menge) || w.menge <= 0)
    return err('menge muss eine positive ganze Zahl sein');
  return OK;
}

/**
 * Prüft eine Rechnungsposition (aus Parser oder Import).
 * @param {{ artikelNr?, menge?, einzelpreis? }} p
 */
export function validatePosition(p) {
  if (!p.artikelNr || !String(p.artikelNr).trim()) return err('artikelNr darf nicht leer sein');
  if (typeof p.menge !== 'number' || p.menge <= 0) return err('menge muss positiv sein');
  if (typeof p.einzelpreis !== 'number' || p.einzelpreis < 0) return err('einzelpreis darf nicht negativ sein');
  return OK;
}

/**
 * Prüft einen Artikel aus dem Katalog.
 * @param {{ artikelNr?, name?, einzelpreis? }} a
 */
export function validateArtikel(a) {
  if (!a.artikelNr  || !String(a.artikelNr).trim())  return err('artikelNr darf nicht leer sein');
  if (!a.name       || !String(a.name).trim())        return err('name darf nicht leer sein');
  if (typeof a.einzelpreis !== 'number' || a.einzelpreis < 0) return err('einzelpreis darf nicht negativ sein');
  return OK;
}
```

- [ ] **Schritt 4: Test im Browser — alle PASS**

- [ ] **Schritt 5: Commit**

```bash
git add src/validation.js tests/test_validation.html
git commit -m "feat: validation.js – zentrale Eingangsvalidierung für Wunsch, Position, Artikel"
```

---

## Task 4: Parser-Rückgabe um warnings/errors erweitern

**Kann parallel zu Tasks 1, 2, 3 laufen.**

**Files:**
- Modify: `src/parser.js`
- Modify: `tests/test_parser.html`

**Problem:** `parseBestellung()` gibt aktuell `{ artikel, ogKosten }` zurück. Warnungen und nicht-parsbare Zeilen werden still verworfen. Import-Fehler sind für den Admin nicht sichtbar.

- [ ] **Schritt 1: Aktuellen Rückgabetyp prüfen**

Lies `src/parser.js` — finde alle `return`-Statements von `parseBestellung`. Notiere die genaue aktuelle Signatur.

- [ ] **Schritt 2: Failing-Tests schreiben**

In `tests/test_parser.html`, füge nach den bestehenden Tests ein:

```js
// Erweiterter Rückgabetyp: warnings und errors
assert('parseBestellung: gibt warnings-Array zurück', (() => {
  const res = parseBestellung('irrelevant text ohne Artikelnummer');
  return Array.isArray(res.warnings);
})());

assert('parseBestellung: gibt errors-Array zurück', (() => {
  const res = parseBestellung('');
  return Array.isArray(res.errors);
})());

assert('parseBestellung: leerer Text → errors enthält Hinweis', (() => {
  const res = parseBestellung('');
  return res.errors.length > 0;
})());
```

- [ ] **Schritt 3: Test im Browser — FAIL (warnings/errors nicht vorhanden)**

- [ ] **Schritt 4: `parseBestellung` erweitern**

In `src/parser.js`, finde die `parseBestellung`-Funktion. Ändere alle Return-Statements so, dass immer `warnings` und `errors` enthalten sind. Minimale Änderung:

Ersetze alle `return { artikel, ogKosten }` durch:
```js
return { artikel, ogKosten, warnings: [], errors: [] };
```

Für den Leerstring-Fall (`!text || !text.trim()`), ersetze das frühe `return` durch:
```js
return { artikel: [], ogKosten: [], warnings: [], errors: ['Kein Text übergeben'] };
```

- [ ] **Schritt 5: Test im Browser — alle PASS**

Alle bisherigen Parser-Tests müssen weiterhin grün sein.

- [ ] **Schritt 6: Commit**

```bash
git add src/parser.js tests/test_parser.html
git commit -m "feat: parseBestellung gibt warnings[] und errors[] zurück"
```

---

## Task 5: `storage.js` — Storage-Wrapper + Refactoring aller HTML-Seiten

**Wave 2: Läuft nach Wave 1, blockiert Tasks 6 und 7.**

**Files:**
- Create: `src/storage.js`
- Create: `tests/test_storage.html`
- Modify: `src/einstellungen-app.js` (leseLokal/schreibeLokal ersetzen)
- Modify: `src/artikel-app.js` (direkte localStorage-Calls ersetzen)
- Modify: `bestellung-neu.html` (ladeJson + alle localStorage-Calls)
- Modify: `bestellung-sammeln.html` (ladeJson + alle localStorage-Calls)
- Modify: `bestellung-abgleich.html` (ladeJson + alle localStorage-Calls)
- Modify: `bestellungen.html` (ladeJson + alle localStorage-Calls)
- Modify: `kassenwart.html` (ladeJson + alle localStorage-Calls)
- Modify: `rechnungen.html` (ladeJson + alle localStorage-Calls)
- Modify: `dashboard.html` (ladeJson + alle localStorage-Calls)
- Modify: `index.html` (ladeJson)

**Bestehendes Muster in jeder HTML-Datei (zu ersetzen):**
```js
// Lesen:
function ladeJson(key) { try { return JSON.parse(localStorage.getItem(key) || 'null'); } catch { return null; } }
// oder inline:
JSON.parse(localStorage.getItem(STORAGE_KEY_B) || 'null')

// Schreiben:
localStorage.setItem(STORAGE_KEY_B, JSON.stringify(bestellungen));
```

- [ ] **Schritt 1: Failing-Tests schreiben**

Erstelle `tests/test_storage.html`:

```html
<!DOCTYPE html>
<html lang="de">
<head><meta charset="UTF-8"><title>Tests – storage</title></head>
<body><pre id="out"></pre>
<script type="module">
import { load, save, remove } from '../src/storage.js';
let ok = 0, fail = 0;
function assert(label, cond) {
  document.getElementById('out').textContent += (cond ? '✅' : '❌') + ' ' + label + '\n';
  cond ? ok++ : fail++;
}

const KEY = '__test_storage__';

// Cleanup vor Tests
remove(KEY);

assert('load: gibt null zurück wenn kein Wert', load(KEY) === null);
assert('save + load: Objekt round-trip', (() => {
  save(KEY, { a: 1, b: 'hallo' });
  const v = load(KEY);
  return v?.a === 1 && v?.b === 'hallo';
})());
assert('save + load: Array round-trip', (() => {
  save(KEY, [1, 2, 3]);
  return JSON.stringify(load(KEY)) === '[1,2,3]';
})());
assert('remove: nach remove ist load null', (() => {
  save(KEY, { x: 1 });
  remove(KEY);
  return load(KEY) === null;
})());
assert('load: gibt null bei korrupten Daten zurück', (() => {
  localStorage.setItem(KEY, 'kein-json{{{');
  const v = load(KEY);
  return v === null;
})());

// Cleanup
remove(KEY);

document.getElementById('out').textContent += `\n${ok} passed, ${fail} failed`;
</script></body></html>
```

- [ ] **Schritt 2: Test im Browser — FAIL**

- [ ] **Schritt 3: `src/storage.js` implementieren**

```js
/**
 * storage.js
 * Einheitlicher Wrapper für localStorage.
 *
 * Öffentliche API:
 *   load(key)        → any | null
 *   save(key, value) → void
 *   remove(key)      → void
 */

/**
 * Lädt einen Wert aus localStorage. Gibt null zurück bei Fehler oder fehlendem Key.
 * @param {string} key
 * @returns {any|null}
 */
export function load(key) {
  try {
    const raw = localStorage.getItem(key);
    return raw ? JSON.parse(raw) : null;
  } catch {
    return null;
  }
}

/**
 * Speichert einen Wert in localStorage.
 * @param {string} key
 * @param {any} value
 */
export function save(key, value) {
  localStorage.setItem(key, JSON.stringify(value));
}

/**
 * Entfernt einen Eintrag aus localStorage.
 * @param {string} key
 */
export function remove(key) {
  localStorage.removeItem(key);
}
```

- [ ] **Schritt 4: Test im Browser — alle PASS**

- [ ] **Schritt 5: `src/einstellungen-app.js` umstellen**

Ersetze in `src/einstellungen-app.js`:
```js
// ALT — imports oben ergänzen:
import { createWebDavClient }                        from './webdav.js';
import { parseMitglieder }                           from './mitglieder.js';
import { ladeDefaultEinstellungen, downloadAlsJson } from './defaults.js';
```
durch:
```js
import { createWebDavClient }                        from './webdav.js';
import { parseMitglieder }                           from './mitglieder.js';
import { ladeDefaultEinstellungen, downloadAlsJson } from './defaults.js';
import { load, save }                                from './storage.js';
```

Ersetze die Funktionen `leseLokal` und `schreibeLokal`:
```js
// ALT:
function leseLokal() {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    return raw ? JSON.parse(raw) : null;
  } catch { return null; }
}
function schreibeLokal(daten) {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(daten));
}

// NEU:
function leseLokal()        { return load(STORAGE_KEY); }
function schreibeLokal(d)   { save(STORAGE_KEY, d); }
```

- [ ] **Schritt 6: `src/artikel-app.js` umstellen**

Füge im Import-Block von `src/artikel-app.js` hinzu:
```js
import { load, save } from './storage.js';
```

Ersetze alle direkten `localStorage.getItem(STORAGE_KEY_E)` durch `load(STORAGE_KEY_E)`,
alle `localStorage.getItem(STORAGE_KEY_A)` durch `load(STORAGE_KEY_A)`,
alle `localStorage.setItem(STORAGE_KEY_A, JSON.stringify(artikel))` durch `save(STORAGE_KEY_A, artikel)`.

- [ ] **Schritt 7: HTML-Seiten umstellen**

Für jede der folgenden Dateien dieselben Schritte:
`bestellung-neu.html`, `bestellung-sammeln.html`, `bestellung-abgleich.html`,
`bestellungen.html`, `kassenwart.html`, `rechnungen.html`, `dashboard.html`, `index.html`

**Vorgehen je Datei:**

1. Im `<script type="module">`-Block, füge ganz oben ein:
   ```js
   import { load, save } from './src/storage.js';
   ```

2. Entferne die lokale `ladeJson`-Hilfsfunktion (sie ist in `storage.js` drin):
   ```js
   // ENTFERNEN:
   function ladeJson(key) { try { return JSON.parse(localStorage.getItem(key) || 'null'); } catch { return null; } }
   ```

3. Ersetze alle `ladeJson(KEY)` durch `load(KEY)`.

4. Ersetze alle:
   ```js
   localStorage.setItem(STORAGE_KEY_B, JSON.stringify(bestellungen));
   ```
   durch:
   ```js
   save(STORAGE_KEY_B, bestellungen);
   ```

5. Dasselbe für `STORAGE_KEY_A` und andere Keys.

- [ ] **Schritt 8: App manuell testen**

Öffne `bestellungen.html` im Browser. Bestellungen müssen laden. Eine Bestellung öffnen/schließen. Keine Konsolenfehler.

- [ ] **Schritt 9: Commit**

```bash
git add src/storage.js tests/test_storage.html src/einstellungen-app.js src/artikel-app.js \
  bestellung-neu.html bestellung-sammeln.html bestellung-abgleich.html bestellungen.html \
  kassenwart.html rechnungen.html dashboard.html index.html
git commit -m "feat: storage.js – einheitlicher localStorage-Wrapper, alle Seiten umgestellt"
```

---

## Task 6: Secrets — NC-Passwort aus localStorage entfernen

**Wave 3: Läuft nach Task 5.**

**Files:**
- Modify: `src/einstellungen-app.js`
- Modify: `einstellungen.html` (Hinweistext)

**Problem:** NC-Passwort wird als Teil von `lo_einstellungen` persistent in `localStorage` gespeichert und ist damit für jeden mit Zugang zum Browser lesbar.

**Lösung:** Passwort nur in `sessionStorage` (gelöscht beim Browser-Schließen) — alle anderen Einstellungen bleiben in `localStorage`.

- [ ] **Schritt 1: Verhalten dokumentieren (kein Test möglich für sessionStorage-Isolation)**

Da sessionStorage kein automatisch testbares Verhalten hat (Seiten-Reload löscht nichts in Tests), manuelle Teststrategie notieren:
- Einstellungen speichern, Seite neu laden → Passwort-Feld leer
- Einstellungen speichern, Tab öffnen lassen → Passwort-Feld noch befüllt
- Browser schließen, neu öffnen → Passwort-Feld leer

- [ ] **Schritt 2: `einstellungen-app.js` ändern**

In `src/einstellungen-app.js`, finde `schreibeLokal(gesamt)` im Speichern-Handler (ca. Zeile 210).

Ersetze die Speicher-Logik so, dass das Passwort **nicht** in `localStorage` landet:

```js
// Finde die Stelle wo gesamt aufgebaut wird (in btn-speichern-Handler):
// ALT:
const gesamt = { ...lokal, ...daten, mitglieder: lokal.mitglieder || [] };
schreibeLokal(gesamt);

// NEU — Passwort vor dem Speichern herausnehmen:
const { nc, ...rest } = { ...lokal, ...daten, mitglieder: lokal.mitglieder || [] };
const ncOhnePass = { url: nc?.url || '', user: nc?.user || '' };
schreibeLokal({ ...rest, nc: ncOhnePass });

// Passwort nur in sessionStorage:
if (nc?.pass) {
  sessionStorage.setItem('lo_nc_pass', nc.pass);
}
```

- [ ] **Schritt 3: Passwort beim Laden aus sessionStorage wiederherstellen**

In der `init()`-Funktion, finde wo die NC-Felder befüllt werden (nach `formularFuellen(e)`).
Ergänze direkt danach:

```js
// Passwort aus sessionStorage wiederherstellen (nicht in localStorage)
const sessionPass = sessionStorage.getItem('lo_nc_pass');
if (sessionPass) {
  const passInput = document.getElementById('nc-pass');
  if (passInput) passInput.value = sessionPass;
}
```

- [ ] **Schritt 4: WebDAV-Client-Erstellung anpassen**

Überall wo der NC-Client gebaut wird, muss das Passwort aus `sessionStorage` kommen wenn `nc.pass` fehlt:

```js
// Finde in einstellungen-app.js die Stelle wo createWebDavClient aufgerufen wird.
// Ändere:
const creds = { url: e.nc.url, user: e.nc.user, pass: e.nc.pass };
// zu:
const pass = e.nc.pass || sessionStorage.getItem('lo_nc_pass') || '';
const creds = { url: e.nc.url, user: e.nc.user, pass };
```

- [ ] **Schritt 5: Hinweis in `einstellungen.html` ergänzen**

Finde das Passwort-Feld (`id="nc-pass"`) in `einstellungen.html`. Füge direkt darunter ein:

```html
<p class="text-sm" style="color:var(--text-2);margin-top:4px">
  Das Passwort wird nur für diese Sitzung gespeichert und beim Schließen des Browsers gelöscht.
</p>
```

- [ ] **Schritt 6: Manuell testen**

1. Einstellungen öffnen, Passwort eingeben, Speichern
2. `localStorage` in DevTools prüfen: `lo_einstellungen.nc.pass` darf nicht vorhanden sein
3. `sessionStorage` prüfen: `lo_nc_pass` muss das Passwort enthalten
4. Tab neu laden: Passwort-Feld muss wieder befüllt sein (aus sessionStorage)
5. Neuen Tab öffnen: Passwort-Feld leer (sessionStorage ist tab-isoliert)

- [ ] **Schritt 7: Commit**

```bash
git add src/einstellungen-app.js einstellungen.html
git commit -m "security: NC-Passwort nicht in localStorage – nur sessionStorage"
```

---

## Task 7: XSS — innerHTML mit Nutzerdaten härten

**Wave 3: Läuft nach Task 5, parallel zu Task 6.**

**Files:**
- Create: `src/dom.js`
- Modify: `src/einstellungen-app.js` (Mitgliederliste-Rendering)
- Modify: `src/artikel-app.js` (Artikel-Tabelle, Import-Vorschau)

**Befund aus Codeanalyse:**
- `einstellungen-app.js:161`: `el.innerHTML = \`...\${m.name}...\`` — Mitgliedername kann `<script>` enthalten
- `artikel-app.js:124`: Artikel-Tabelle mit `${a.name}`, `${a.artikelNr}` — importierte Daten
- `artikel-app.js:173`: Import-Vorschau mit geparsten Artikelnamen

**Nicht betroffen (statisches HTML ohne Nutzerdaten):**
- `innerHTML = ''` (leeren)
- `innerHTML` mit reinen Zahlen (formatierte Preise via `eur()`-Funktion)
- `innerHTML` mit statischen Strings wie Error-Meldungen in deutschem Klartext

- [ ] **Schritt 1: `src/dom.js` erstellen und testen**

Erstelle zuerst die Test-Datei `tests/test_dom.html`:

```html
<!DOCTYPE html>
<html lang="de">
<head><meta charset="UTF-8"><title>Tests – dom</title></head>
<body><pre id="out"></pre>
<script type="module">
import { esc, el, setText } from '../src/dom.js';
let ok = 0, fail = 0;
function assert(label, cond) {
  document.getElementById('out').textContent += (cond ? '✅' : '❌') + ' ' + label + '\n';
  cond ? ok++ : fail++;
}

// esc()
assert('esc: kein HTML-Zeichen → unverändert', esc('Hallo Welt') === 'Hallo Welt');
assert('esc: < wird escaped', esc('<script>') === '&lt;script&gt;');
assert('esc: & wird escaped', esc('A & B') === 'A &amp; B');
assert('esc: " wird escaped', esc('"Hallo"') === '&quot;Hallo&quot;');
assert('esc: null → leerer String', esc(null) === '');
assert('esc: undefined → leerer String', esc(undefined) === '');

// el()
assert('el: erstellt Element mit Tag', (() => {
  const d = el('div');
  return d instanceof HTMLElement && d.tagName === 'DIV';
})());
assert('el: setzt Text-Content sicher', (() => {
  const span = el('span', { text: '<b>böse</b>' });
  return span.textContent === '<b>böse</b>' && span.innerHTML === '&lt;b&gt;böse&lt;/b&gt;';
})());
assert('el: setzt className', (() => {
  const d = el('div', { className: 'badge badge-blue' });
  return d.className === 'badge badge-blue';
})());

// setText()
assert('setText: setzt textContent', (() => {
  const d = document.createElement('div');
  setText(d, 'Hallo <Welt>');
  return d.textContent === 'Hallo <Welt>' && !d.innerHTML.includes('<Welt>');
})());

document.getElementById('out').textContent += `\n${ok} passed, ${fail} failed`;
</script></body></html>
```

- [ ] **Schritt 2: Test im Browser — FAIL**

- [ ] **Schritt 3: `src/dom.js` implementieren**

```js
/**
 * dom.js
 * Sichere DOM-Hilfsfunktionen — kein innerHTML mit Nutzerdaten.
 *
 * Öffentliche API:
 *   esc(value)                    → HTML-escaped String
 *   el(tag, opts?)                → HTMLElement
 *   setText(element, value)       → void
 */

/**
 * Escaped einen Wert für sicheres Einfügen in HTML-Text.
 * Null/undefined → leerer String.
 * @param {any} value
 * @returns {string}
 */
export function esc(value) {
  if (value == null) return '';
  return String(value)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;');
}

/**
 * Erstellt ein DOM-Element mit optionalen Eigenschaften.
 * Text wird immer über textContent gesetzt, nie innerHTML.
 *
 * @param {string} tag             HTML-Tag (z.B. 'div', 'span', 'td')
 * @param {{ text?, className?, style?, title? }} [opts]
 * @returns {HTMLElement}
 */
export function el(tag, opts = {}) {
  const element = document.createElement(tag);
  if (opts.text      != null) element.textContent = String(opts.text);
  if (opts.className != null) element.className   = opts.className;
  if (opts.style     != null) element.style.cssText = opts.style;
  if (opts.title     != null) element.title = opts.title;
  return element;
}

/**
 * Setzt den Text eines Elements sicher (via textContent).
 * @param {HTMLElement} element
 * @param {any} value
 */
export function setText(element, value) {
  element.textContent = value != null ? String(value) : '';
}
```

- [ ] **Schritt 4: Test im Browser — alle PASS**

- [ ] **Schritt 5: `einstellungen-app.js` — Mitgliederliste sichern**

In `src/einstellungen-app.js`, finde die Funktion `zeigeNcMitglieder` (ca. Zeile 158).

Ergänze den Import:
```js
import { esc } from './dom.js';
```

Ersetze die `el.innerHTML`-Zuweisung:
```js
// ALT:
el.innerHTML = `
  <p class="text-sm" style="margin-bottom:8px">${mitglieder.length} Mitglieder geladen:</p>
  <div style="display:flex;flex-wrap:wrap;gap:6px">
    ${mitglieder.map(m =>
      `<span class="badge badge-blue">${m.name}</span>`
    ).join('')}
  </div>`;

// NEU:
el.innerHTML = '';
const p = document.createElement('p');
p.className = 'text-sm';
p.style.marginBottom = '8px';
p.textContent = `${mitglieder.length} Mitglieder geladen:`;
el.appendChild(p);

const flex = document.createElement('div');
flex.style.cssText = 'display:flex;flex-wrap:wrap;gap:6px';
for (const m of mitglieder) {
  const span = document.createElement('span');
  span.className = 'badge badge-blue';
  span.textContent = m.name;
  flex.appendChild(span);
}
el.appendChild(flex);
```

- [ ] **Schritt 6: `artikel-app.js` — kritische innerHTML-Stellen sichern**

In `src/artikel-app.js`, füge den Import hinzu:
```js
import { esc } from './dom.js';
```

Finde alle Stellen in der Artikel-Tabelle wo `${a.name}`, `${a.artikelNr}`, `${a.variante}` in innerHTML-Templates eingesetzt werden. Ersetze diese Template-Literale durch `${esc(a.name)}`, `${esc(a.artikelNr)}`, `${esc(a.variante)}`.

Gleiches für die Import-Vorschau-Tabelle mit geparsten Artikel-Daten.

Beispiel:
```js
// ALT:
`<td>${a.name}</td>`
// NEU:
`<td>${esc(a.name)}</td>`
```

- [ ] **Schritt 7: Manuell testen**

Artikel mit Name `<img src=x onerror=alert(1)>` anlegen. Name muss als Text angezeigt werden, kein Alert ausgelöst.

- [ ] **Schritt 8: Commit**

```bash
git add src/dom.js tests/test_dom.html src/einstellungen-app.js src/artikel-app.js
git commit -m "security: dom.js – XSS-sichere Render-Helfer, innerHTML mit Nutzerdaten ersetzt"
```

---

## Task 8: Rechnungen auditierbar machen

**Wave 4: Kann nach Task 5 laufen.**

**Files:**
- Create: `src/audit.js`
- Create: `tests/test_audit.html`

**Ziel:** Rechnungen dürfen nach Erstellung nicht mehr geändert werden. Eine Stornierung erzeugt eine neue Gegenbuchung, keine Mutation. Zusätzlich: einfaches Änderungsprotokoll für kritische Aktionen.

- [ ] **Schritt 1: Failing-Tests schreiben**

Erstelle `tests/test_audit.html`:

```html
<!DOCTYPE html>
<html lang="de">
<head><meta charset="UTF-8"><title>Tests – audit</title></head>
<body><pre id="out"></pre>
<script type="module">
import { logAktion, ladeLog, istRechnungUnveraendert } from '../src/audit.js';
let ok = 0, fail = 0;
function assert(label, cond) {
  document.getElementById('out').textContent += (cond ? '✅' : '❌') + ' ' + label + '\n';
  cond ? ok++ : fail++;
}

// logAktion
const eintrag = logAktion('RECHNUNG_ERSTELLT', 'martin_kammler', { rechnungId: 'r1', betrag: 49.90 });
assert('logAktion: gibt Objekt zurück', typeof eintrag === 'object');
assert('logAktion: enthält action', eintrag.action === 'RECHNUNG_ERSTELLT');
assert('logAktion: enthält user', eintrag.user === 'martin_kammler');
assert('logAktion: enthält timestamp (ISO)', typeof eintrag.timestamp === 'string' && eintrag.timestamp.includes('T'));
assert('logAktion: enthält changes', eintrag.changes?.rechnungId === 'r1');

// istRechnungUnveraendert
const rechnung = { id: 'r1', nummer: 'R_2025_01_001', gesamtbetrag: 49.90, positionen: [{ name: 'T-Shirt', menge: 1 }] };
assert('istRechnungUnveraendert: gleiche Rechnung → true', istRechnungUnveraendert(rechnung, rechnung));
assert('istRechnungUnveraendert: geänderter Betrag → false', (() => {
  const geaendert = { ...rechnung, gesamtbetrag: 99.90 };
  return istRechnungUnveraendert(rechnung, geaendert) === false;
})());
assert('istRechnungUnveraendert: geänderte Positionen → false', (() => {
  const geaendert = { ...rechnung, positionen: [{ name: 'Hose', menge: 1 }] };
  return istRechnungUnveraendert(rechnung, geaendert) === false;
})());

document.getElementById('out').textContent += `\n${ok} passed, ${fail} failed`;
</script></body></html>
```

- [ ] **Schritt 2: Test im Browser — FAIL**

- [ ] **Schritt 3: `src/audit.js` implementieren**

```js
/**
 * audit.js
 * Unveränderlichkeits-Prüfung für Rechnungen und einfaches Aktionsprotokoll.
 *
 * Öffentliche API:
 *   logAktion(action, user, changes) → AuditEintrag
 *   ladeLog()                        → AuditEintrag[]
 *   istRechnungUnveraendert(a, b)    → boolean
 */

const AUDIT_KEY = 'lo_audit_log';

/**
 * Schreibt einen Audit-Eintrag in localStorage.
 * @param {string} action    z.B. 'RECHNUNG_ERSTELLT', 'ZAHLUNG_GESETZT'
 * @param {string} user      mitgliedId des ausführenden Nutzers
 * @param {object} changes   Freitextdaten zur Aktion
 * @returns {object}         Der gespeicherte Eintrag
 */
export function logAktion(action, user, changes = {}) {
  const eintrag = {
    action,
    user,
    timestamp: new Date().toISOString(),
    changes,
  };
  try {
    const log = ladeLog();
    log.push(eintrag);
    // Maximal 500 Einträge behalten (FIFO)
    if (log.length > 500) log.splice(0, log.length - 500);
    localStorage.setItem(AUDIT_KEY, JSON.stringify(log));
  } catch {
    // Logging darf niemals die App blockieren
  }
  return eintrag;
}

/**
 * Lädt alle gespeicherten Audit-Einträge.
 * @returns {object[]}
 */
export function ladeLog() {
  try {
    return JSON.parse(localStorage.getItem(AUDIT_KEY) || '[]');
  } catch {
    return [];
  }
}

/**
 * Prüft ob zwei Rechnungs-Objekte inhaltlich identisch sind
 * (vergleicht die prüfrelevanten Felder: nummer, gesamtbetrag, positionen).
 * @param {object} a  Original-Rechnung
 * @param {object} b  Zu prüfende Version
 * @returns {boolean}
 */
export function istRechnungUnveraendert(a, b) {
  if (a === b) return true;
  if (a.nummer       !== b.nummer)       return false;
  if (a.gesamtbetrag !== b.gesamtbetrag) return false;
  if (JSON.stringify(a.positionen) !== JSON.stringify(b.positionen)) return false;
  return true;
}
```

- [ ] **Schritt 4: Test im Browser — alle PASS**

- [ ] **Schritt 5: Commit**

```bash
git add src/audit.js tests/test_audit.html
git commit -m "feat: audit.js – Aktionsprotokoll und Rechnungs-Unveränderlichkeit"
```

---

## Self-Review

### Spec-Coverage gegen `docs/plans/haertung.md`:

| Item | Task | Abgedeckt? |
|---|---|---|
| 1. Secrets/Zugangsdaten | Task 6 | ✅ |
| 2. XSS-Schutz | Task 7 | ✅ |
| 3. Zentrale Validierung | Task 3 | ✅ |
| 4. Förderlogik clamping | Task 1 | ✅ |
| 5. Datums-Handling | Task 2 | ✅ |
| 6. Inline-Skripte auslagern | Task 5 (Teilweise — in storage-Schritt) | ⚠️ Storage-Wrapper lagert Logik aus HTML aus, vollständige *-app.js-Auslagerung ist Folge-Schritt |
| 8. Storage-Wrapper | Task 5 | ✅ |
| 9. Sync-Strategie | — | ⏭ Bewusst nicht geplant (zu groß für diesen Umfang, eigener Plan empfohlen) |
| 10. Parser warnings/errors | Task 4 | ✅ |
| 11. Rechnungen auditierbar | Task 8 | ✅ |
| 13. Logging | Task 8 | ✅ |

### Placeholder-Scan: keine gefunden.

### Type-Konsistenz: alle Funktionssignaturen konsistent über Tasks.

---

## Ausführungsreihenfolge für Subagents

```
Wave 1 (parallel starten):
  Subagent A → Task 1 + Task 2
  Subagent B → Task 3
  Subagent C → Task 4

Wave 2 (nach Wave 1, blockiert alles Folgende):
  Subagent D → Task 5 (storage.js + alle HTML-Seiten)

Wave 3 (nach Task 5, parallel):
  Subagent E → Task 6 (Secrets)
  Subagent F → Task 7 (XSS + dom.js)

Wave 4 (nach Wave 3):
  Subagent G → Task 8 (Audit)
```